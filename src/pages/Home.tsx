import React, { FC } from "react";
import Header from "../component/header";

const Home: FC = () => {
  return (
    <section>
      <Header title='Home Page' />
      <div>Home Page</div>
    </section>
  );
};

export default Home;
